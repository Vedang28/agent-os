Verify the current phase's exit gate in docs/PHASE_STATUS.md.

For EACH checkbox: run the real check (tests, compile, run the code) — do NOT assume. Report pass/fail per item with evidence.

- Only if ALL items pass: mark the phase complete, advance PHASE_STATUS.md to the next phase, then run /log.
- If ANY item fails: write the blocker to PHASE_STATUS.md (Notes/blockers), do NOT advance, and follow docs/WHEN_STUCK.md.
